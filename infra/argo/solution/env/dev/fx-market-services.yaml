# Source: application/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: flink-orchestrator-svc
  labels:
    managed-by: Helm
    release: fx-market-services
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: flink-orchestrator
---
# Source: application/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: fx-market-connector-svc
  labels:
    managed-by: Helm
    release: fx-market-services
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: fx-market-connector
---
# Source: application/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: fx-market-processor-svc
  labels:
    managed-by: Helm
    release: fx-market-services
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: fx-market-processor
---
# Source: application/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flink-orchestrator
  labels:
    app: flink-orchestrator
    managed-by: Helm
    release: fx-market-services
    version: 1.0.0
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flink-orchestrator
      release: fx-market-services
  template:
    metadata:
      labels:
        app: flink-orchestrator
        release: fx-market-services
        version: 1.0.0
    spec:
      containers:
        - name: flink-orchestrator
          image: "localhost:5001/fx-market-services/flink-orchestrator:1.0.0"
          imagePullPolicy: IfNotPresent
          env:
          - name: SERVER_PORT
            value: "8080"
          - name: FLINK_URL
            value: http://fx-flink-jobmanager:8081
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
              scheme: HTTP
            failureThreshold: 3
            initialDelaySeconds: 120
            periodSeconds: 15
            successThreshold: 1
            timeoutSeconds: 5
          resources:
            limits:
              cpu: 1
              memory: 3584Mi
            requests:
              cpu: 500m
              memory: 3584Mi
          volumeMounts:
      topologySpreadConstraints:
      - maxSkew: 3
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            release: fx-market-services
      volumes:
---
# Source: application/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fx-market-connector
  labels:
    app: fx-market-connector
    managed-by: Helm
    release: fx-market-services
    version: 1.0.0
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fx-market-connector
      release: fx-market-services
  template:
    metadata:
      labels:
        app: fx-market-connector
        release: fx-market-services
        version: 1.0.0
    spec:
      containers:
        - name: fx-market-connector
          image: "localhost:5001/fx-market-services/fx-market-connector:1.0.0"
          imagePullPolicy: IfNotPresent
          env:
          - name: SERVER_PORT
            value: "8080"
          - name: KAFKA_BOOTSTRAP_SERVERS
            value: PLAINTEXT://kafka:9092
          - name: VENDOR_MARKET_DATA_STUB_URL
            value: http://fx-market-data-stub-svc:3080
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
              scheme: HTTP
            failureThreshold: 3
            initialDelaySeconds: 120
            periodSeconds: 15
            successThreshold: 1
            timeoutSeconds: 5
          resources:
            limits:
              cpu: 1
              memory: 3072Mi
            requests:
              cpu: 500m
              memory: 3072Mi
          volumeMounts:
      topologySpreadConstraints:
      - maxSkew: 3
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            release: fx-market-services
      volumes:
---
# Source: application/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fx-market-processor
  labels:
    app: fx-market-processor
    managed-by: Helm
    release: fx-market-services
    version: 1.0.0
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fx-market-processor
      release: fx-market-services
  template:
    metadata:
      labels:
        app: fx-market-processor
        release: fx-market-services
        version: 1.0.0
    spec:
      containers:
        - name: fx-market-processor
          image: "localhost:5001/fx-market-services/fx-market-processor:1.0.0"
          imagePullPolicy: IfNotPresent
          env:
          - name: SERVER_PORT
            value: "8080"
          - name: KAFKA_BOOTSTRAP_SERVERS
            value: PLAINTEXT://kafka:9092
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
              scheme: HTTP
            failureThreshold: 3
            initialDelaySeconds: 120
            periodSeconds: 15
            successThreshold: 1
            timeoutSeconds: 5
          resources:
            limits:
              cpu: 1
              memory: 3072Mi
            requests:
              cpu: 500m
              memory: 3072Mi
          volumeMounts:
      topologySpreadConstraints:
      - maxSkew: 3
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            release: fx-market-services
      volumes:

