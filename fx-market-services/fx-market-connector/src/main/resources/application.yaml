spring:
  application:
    name: fxMarketDataConnector

server:
  port: 4080

kafka:
  bootstrap-servers: localhost:9092
  topic: fx_rates_kafka

vendor:
  market:
    data:
      stub:
        url: "http://localhost:3080"
