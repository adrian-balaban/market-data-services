spring:
  application:
    name: fxMarketDataProcessor

server:
  port: 4081

kafka:
  bootstrap-servers: PLAINTEXT://localhost:9092
  topic: fx_rates
  application-id-config: fx-market-processor-stream-operator