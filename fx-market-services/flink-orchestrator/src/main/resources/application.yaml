spring:
  application:
    name: FlinkOrchestrator

server:
  port: 4082

flink:
  url: http://localhost:8081
  jobs:
    - name: fxmarketflinkproces
      arguments:
        topic: fx_rates
        bootstrapServers: kafka:9092
    - name: fxmarketflinkconnector
      arguments:
        stub_hostname: fx-market-data-stub-ws-svc
        stub_port: 3081
