spring:
  application:
    name: FlinkOrchestrator

server:
  port: 8080

flink:
  url: "http://fx-flink-jobmanager:8081"
  jobs:
    - name: fxmarketflinkproces
      arguments:
        topic: fx_rates
        bootstrapServers: kafka:9071
        redisHost: fx-redis-cluster
        redisPort: 6379
        redisUsername: flink-sink
        redisPassword: default